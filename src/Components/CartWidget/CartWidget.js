import React from 'react'
import { BsCart } from "react-icons/bs";;

const CartWidget = () => {
  return (
    <div>
        <button>
          <BsCart/>
        </button>
    </div>
  )
}

export default CartWidget